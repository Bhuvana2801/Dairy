rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    

    // Rules for Admin Sections (Reports, Expenses, etc.)
    match /daily/{document}      { allow read, write: if true; }
    match /expenses/{document}   { allow read, write: if true; }
    match /production/{document} { allow read, write: if true; }
    match /salaries/{document}   { allow read, write: if true; }
    match /orders/{document}     { allow read, write: if true; }
    match /products/{document}   { allow read, write: if true; }
    match /customers/{document}  { allow read, write: if true; }
    match /order_counters/{document} { allow read, write: if true; }
    match /monthly_customers_{monthYear}/{document} { allow read, write: if true; }
    
    // Default: deny everything else
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
